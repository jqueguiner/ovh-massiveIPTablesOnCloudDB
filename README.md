# ovh-massiveIPTablesOnCloudDB
* Please install requirements :

```
pip install -r requirements.txt
```

* adjust the whitelist by editing the ip-ranges.json file

* apply the whitelisting
```
python whitelist_ips.py
```
